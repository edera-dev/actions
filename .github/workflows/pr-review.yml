name: PR review

# The two advisory review checks, run through the shared workflow this
# repository hosts, at the version on the pull request's own branch: a review
# of the diff, and a review of whether the tests would catch the change being
# wrong. Both are comment-only and never block a merge. What they look for in
# this repository is in .github/review/.
#
# Each check needs the four PR_REVIEW_* variables set on this repository.
# Without them both checks skip quietly.

on:
  pull_request:
    types: [opened, synchronize, ready_for_review]
    branches: [main]

permissions:
  contents: read

concurrency:
  group: pr-review-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  suggestions:
    uses: ./.github/workflows/advisory-review.yml
    permissions:
      contents: read
      pull-requests: write
      id-token: write
    with:
      check: pr-review-suggestions
      federation-rule-id: ${{ vars.PR_REVIEW_FEDERATION_RULE_ID }}
      organization-id: ${{ vars.PR_REVIEW_ORGANIZATION_ID }}
      service-account-id: ${{ vars.PR_REVIEW_SERVICE_ACCOUNT_ID }}
      workspace-id: ${{ vars.PR_REVIEW_WORKSPACE_ID }}

  coverage:
    uses: ./.github/workflows/advisory-review.yml
    permissions:
      contents: read
      pull-requests: write
      id-token: write
    with:
      check: pr-test-coverage
      federation-rule-id: ${{ vars.PR_REVIEW_FEDERATION_RULE_ID }}
      organization-id: ${{ vars.PR_REVIEW_ORGANIZATION_ID }}
      service-account-id: ${{ vars.PR_REVIEW_SERVICE_ACCOUNT_ID }}
      workspace-id: ${{ vars.PR_REVIEW_WORKSPACE_ID }}
